## ADDED Requirements

### Requirement: Interactive Timeline
The system SHALL display KG events on a visual timeline (Plottr-style), filterable by character, location, and tag. Events SHALL be draggable for reordering.

#### Scenario: Filter timeline by character
- **WHEN** user selects character "张三" in the timeline filter
- **THEN** only events involving "张三" are displayed on the timeline
